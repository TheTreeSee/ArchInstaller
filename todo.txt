ToDo:
firewalld over ufw?
gvfs gvfs-mtp
xdg-user-dirs network-manager
install essential funciton os-prober on dualboot if (now on servermode false)
change set keymap function and fix keyboard laguage us be azerty qwerty support
change server mode to mode and check for desktop, server, ..?

make swapsize work with 100% rootsize
fix ssh working out of box
fix dual boot partition table stuffs
fix network and configure network funciton
add custom package install field in settings
option REMOVE_INSTALLERS to remove install files or leave them in home?

for simple network setup:
- /etc/systemd/network/filename.network
```
[Match]
Name=eth0

[Network]
DHCP=yes
```

Doing:

install postinstall and make it run on boot.
at end of postinstall disable run on boot.


ToTest:
check if 100% works for rootsize
